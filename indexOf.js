// indexOf() method
//returns the first index at which a given element can be found in the array, or -1 if is not present

const indexOfDemoArr = [10, 20, 30, 40, 50];
const index = indexOfDemoArr.indexOf(30);
console.log(index);
