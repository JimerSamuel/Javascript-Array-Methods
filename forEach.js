// forEach()
// The forEach() method executes a provided functions once for each array element

const forEachDemoArr = [10, 20, 30, 40, 50];
forEachDemoArr.forEach((val) => {
  console.log(val + 3);
});
